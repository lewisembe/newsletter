# Guía para agentes

- Propósito: suite de tests Pytest para validar DB y etapa 01.
- Archivos: `test_db.py` verifica conexiones/consultas; `test_extract_urls.py` cubre scraping y filtrado inicial.
- Uso: ejecutar con `pytest -v tests/`; asegura que las rutas de datos y configs estén preparadas (drivers Selenium y fuentes habilitadas).
