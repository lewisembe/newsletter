# Guía para agentes

- Propósito: espacio para utilidades compartidas del backend que no encajan en `app/`.
- Estado: actualmente vacío; úsalo para helpers genéricos si crece la base de código.
- Nota: al agregar módulos aquí, documenta APIs públicas y evita dependencias circulares con `app/`.
