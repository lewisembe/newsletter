# Guía para agentes

- Propósito: plantilla para configuraciones adicionales del backend (p.ej., archivos YAML/JSON específicos de la API).
- Estado: carpeta vacía; crea aquí configs separadas si no van en variables de entorno.
- Nota: evita almacenar secretos sin cifrado; si añades archivos, referencia rutas claras en `app/config.py` o env vars.
